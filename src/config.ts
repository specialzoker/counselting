// 판정 라벨 경계값 (차이값 = 학생백분위 − 대학 70%컷 기준). 상담 편의를 위한 보조 지표이며
// 원본 엑셀에는 없는 추가 기능이다. 운영하며 조정 가능.
export const LABEL_THRESHOLDS = { stable: 5, fit: 0, brave: -3 }

// 교과·종합전형 판정: 학생 내신 등급이 70%컷보다 이만큼(등급) 나빠도 "소신"으로 본다.
// 이 값을 넘어서면 "도전". 조정 가능.
export const GYOGWA_MARGIN = 0.5
